---
import Base from "../Modal.astro";
import { random } from "../../libs/random";

export const MODAL_ID = random();
---

<Base id={MODAL_ID}>
  <div class="container">
    <div class="content">
      <h1>다음 방법을 시도해보세요</h1>
      <p>학교에서 지급한 계정이 맞는지 확인해보세요.</p>
      <p>인터넷 환경이 원활한지 확인해보세요.</p>
      <p>
        학교 또는 <a
          target="_blank"
          href="https://www.instagram.com/sunrin_official/">학생회</a
        >에 문의해보세요.
      </p>
    </div>
    <button onclick={`closeModal('${MODAL_ID}')`}>시도해볼게요!</button>
  </div>
</Base>

<style lang="scss">
  .container {
    width: 100%;
    height: 100%;

    display: flex;
    flex-direction: column;
    gap: 20px;
  }

  .content {
    display: flex;
    padding: 8px 4px;
    flex-direction: column;
    justify-content: center;
    align-items: flex-start;
    gap: 8px;

    h1 {
      margin-bottom: 12px;
      color: var(--Grayscale-50);
      font-size: 20px;
      font-weight: 600;
    }

    p {
      color: var(--Grayscale-30);
      font-size: 15px;
      font-weight: 400;
    }

    p:before {
      content: "•";
      margin-right: 8px;
    }
  }

  button {
    padding: 16px 0px;
    color: var(--Grayscale-10);
    border-radius: 4px;
    background: var(--Grayscale-50);
    font-size: 17px;
    font-weight: 600;
  }
</style>
